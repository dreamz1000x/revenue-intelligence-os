import { Metric, PageHeader, RecordView } from "@/components/ui";
import { getPrincipal, riosFetch } from "@/lib/api";
import { hasRole } from "@/lib/roles";
export default async function Operations(){const principal=await getPrincipal();const [health,ready]=await Promise.all([riosFetch<Record<string,unknown>>("/health",{},false),riosFetch<Record<string,unknown>>("/ready",{},false)]);const metrics=hasRole(principal.roles,"admin")?await riosFetch<Record<string,unknown>>("/metrics"):null;return <><PageHeader eyebrow="Operations" title="Service posture" copy="Live process and database readiness from the deployed API. Metrics remain an admin-only operational surface."/><section className="metrics"><Metric label="API health" value={String(health.status)}/><Metric label="Database readiness" value={String(ready.status)}/><Metric label="Metrics access" value={metrics?"Admin enabled":"Admin only"}/></section>{metrics&&<section className="panel"><h2>Current process metrics</h2><RecordView data={metrics}/></section>}</>}
